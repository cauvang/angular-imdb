import { GetSectionNamePipe } from './get-section-name.pipe';

describe('GetSectionNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetSectionNamePipe();
    expect(pipe).toBeTruthy();
  });
});
